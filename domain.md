# 印书局域模型

产品的统一语言。版式学完整术语表见 [版式规范研究文档](.scratch/charter/research/01-banshi-guifan.md)，此处只收产品代码要用的词。

## 版面

- **半叶**：渲染的基本单位，一个筒子页的一半——app 里"一页"即一个半叶。对开视图 = 两个半叶。
- **版框**：半叶四周的边框。**界行**：列与列之间的竖线。**版心**：中央窄列，容书名/卷次/页码/**鱼尾**（▼形标记，单黑鱼尾为默认）。
- **天头/地脚**：框外上/下留白，比例约 1.2–1.5 : 1。
- **行格**：半叶 N 行 × 每行 M 字的网格制式（默认域：8–15 行 × 16–22 字）。布局引擎的一切定位都从行格网格推出。

## 文字层

- **正文**：占整字位的大字。
- **夹注**：双行小字注（半字号、右行先、注毕回单行）。输入用圆括号表达。
- **句读**：句=小圈○、读=点丶，附着于字右下角、不占字位。来源=现代标点自动映射（。！？→句；，、；：→读）。
- **篇题**：`#` 行，自成一列。**提行**：空行=段落起新列。
- **版**（导出语境）：一次导出的一幅画面——单叶模式一版=一半叶，对开模式一版=一整叶（两半叶）。
- （远期）**旁注/眉批**：列旁/天头的批注层，v1 不做，语法留扩展位。

## 输入→渲染管线

文本处理顺序：**句读映射 →（可选）简转繁 s2t →（可选）旧字形映射 → 布局引擎（网格→逐字坐标表）→ SVG 渲染 → 三出口**（屏幕阅读 / PNG 导出 / pdf-lib PDF）。

## 决策记录

- 2026-07-31 输入格式 v1：元数据走 UI 表单不进正文；圆括号=夹注；现代标点映射句读；结构只认空行与 `#`。详见 [docs/markup-v1.md](docs/markup-v1.md)。
